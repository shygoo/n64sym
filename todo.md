# Todo
```
n64sym:
  dump symbols from relocations when signature scanning
  
n64sig:
  fix labelling for hi16/lo16 relocations

builtin signatures
  include libn_audio for OS versions <= 2.0i?
  include nusys libraries
```